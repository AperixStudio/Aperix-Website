/**
 * Mobile overrides — tune via /dev/hero-canvas (mobile preview + Copy mobile overrides).
 */
export const MOBILE_ACT3_REVEAL_OVERRIDES = {
  iphoneEndX: 0.49,
  iphoneEndY: 0.28,
  monitorScale: 0.9,
  monitorEndX: -0.61,
  monitorEndY: 0.42,
  cameraEndY: 0.48,
  cameraEndZ: 3.6,
  lookAtEndY: 0.02,
};
